<div class="container">
    <h1>{{ titreForm }}</h1>
    <center>
    <div class="col-6 text-center">
        {{ form_start(form)}}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">{{ btnTitle }}</button>
        {{ form_end(form) }}
        </div>
        <a class="btn btn-info m-3" href="{{ path('list_produit') }}">back to list</a>   
    </center>
</div>