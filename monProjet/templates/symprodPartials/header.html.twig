<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="navbar-collapse collapse show" id="navbarColor01" style="">
        <ul class="navbar-nav mr-auto">
        
        <li class="nav-item">
            <a class="nav-link" href={{ path('index_produit') }}>Exercice Chakib</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href={{ path('symprod') }}>Page d'accueil</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href={{ path('symprod_ad') }}>Annonces</a>
        </li>
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
  <div class="container">
    <div class="alert alert-{{ label }}">
      {% for message in messages %}
      <p> {{ message | raw }}
      {% endfor %}
    </div>
  </div>
{% endfor %}